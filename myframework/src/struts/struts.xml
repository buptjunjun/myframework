<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

    <constant name="struts.enable.DynamicMethodInvocation" value="false" />
    <constant name="struts.devMode" value="false" />
	<constant name="struts.action.extension" value="html,json"/>
	<constant name="struts.ognl.allowStaticMethodAccess" value="true" />
	<constant name="struts.multipart.maxSize" value="1048576000" />			
	<constant name="struts.custom.i18n.resources" value="message"></constant>	 
	
	<!-- <constant name="struts.action.excludePattern" value="/baijuzhan/js/*"/>  -->
    <package name="default" namespace="/" extends="struts-default">

		<interceptors>
			<interceptor name="exceptionIntercepter" class="com.zgwangbo.framework.web.ExceptionInterceptor" />
			
			<interceptor-stack name="globalIntercepter">
				<interceptor-ref name="exceptionIntercepter" />
				<interceptor-ref name="fileUpload">
			     <!-- 默认附件大小100M -->
			     <param name="maximumSize">104857600</param>
			    </interceptor-ref>
				<interceptor-ref name="defaultStack" />
			</interceptor-stack>
		</interceptors>
		
		<default-interceptor-ref name="globalIntercepter" />
        
        <default-action-ref name="index" />

        <global-results>
            <result name="error">/error.jsp</result>
        </global-results>
 
     <!--  <global-exception-mappings>
            <exception-mapping exception="java.lang.Exception" result="error"/>
        </global-exception-mappings>

        <action name="index">
            <result type="redirectAction">
                <param name="actionName">index</param>
                <param name="namespace">/</param>
            </result>
        </action> -->

    </package>

    <!-- Add packages here -->
	
	<include file="main.xml"/>
</struts>
